# Next.js instance wired to the Django backend. Copy to frontend-django.env.
NEXT_PUBLIC_API_BASE_URL=/api                       # browser -> same-origin via nginx (no CORS)
INTERNAL_API_URL=http://backend-django:8000         # SSR -> docker network
NEXT_PUBLIC_BACKEND_LABEL=Django REST Framework
WATCHPACK_POLLING=true
