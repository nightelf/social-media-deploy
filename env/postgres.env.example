# Postgres container config. Copy to postgres.env and adjust as needed.
POSTGRES_USER=social
POSTGRES_PASSWORD=social_dev_password
# db-init.sh creates these two databases on first boot:
POSTGRES_DB=social
EXTRA_DATABASES=social_fastapi,social_django
